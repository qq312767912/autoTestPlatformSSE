# 统一代码审查流水线需求

1. 快速、标准、深度模式均应对全部可分析 Diff 执行确定性规则扫描。
2. 系统应优先使用 Tree-sitter 按 AST 声明切分代码，缺少解析器时明确降级。
3. 系统应基于声明和调用标识符检索相关源码，标准和深度模式将其作为 LLM 上下文。
4. 系统应以可插拔方式运行 Semgrep、项目 Linter，工具缺失不得阻塞报告。
5. 标准模式应融合规则与 LLM 结果；深度模式应增加 OpenCodeReview 且只补审失败范围。
6. PR/MR 回写必须为显式操作，默认只生成平台及 HTML 报告。
